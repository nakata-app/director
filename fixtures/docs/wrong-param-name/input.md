## `submit_task(env, prompt, username, aspect_ratio)`

Submits a generation task to NanaBanana.

**Parameters**
- `env`: worker environment bindings
- `prompt`: image generation prompt
- `user` (string): the requesting user's name
- `aspect_ratio` (string): e.g. `"1:1"`, `"16:9"`
